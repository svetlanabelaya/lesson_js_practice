///////////////////Исключительные ситуации//////////////////////////////

//////////Введение в исключительные ситуации в JavaScript//////////////
/*
 В JavaScript очень мало ситуаций, в которых возникают исключения. Во-первых, просто потому, что очень мало мест для их возникновения.

Во-вторых, потому, что сам язык "всепрощающий": он на многое смотрит сквозь пальцы, например, на деление на ноль или на некорректный HTML код. Даже если вы укажете неверный путь к картинке, которую собираетесь загрузить, JavaScript вам простит и это и не посчитает исключением.

Однако, исключительные ситуации есть. Мы разберем две самые простые и на их примере изучим работу с исключениями в JavaScript.

Первое исключение возникает, когда мы хотим разобрать некорректный JSON:
let data = JSON.parse('{1,2,3,4,5}'); // данный json некорректен

А второе исключение возникает, когда локальное хранилище, выделенное под наш сайт переполняется (больше 5 мегабайт). Давайте искусственно вызовем такое исключение:
let str = '';
for (let i = 1; i <= 6 * 10 ** 6; i++) { // формируем строку более 5 мб
	str += '+';
}

localStorage.setItem('key', str); // пытаемся записать в хранилище

Как JavaScript реагирует на такие исключительные ситуации? Он просто вываливает ошибку в консоль и прекращает дальнейшее выполнение скрипта.

Наша задача, как программистов, состоит в том, чтобы отловить такую ситуацию и как-то справится с ней, не давая программе совсем прекратить свое выполнение. Для этого существуют специальные конструкция try-catch, которую мы разберем в следующих уроках. 
*/

/*
Создайте строку достаточно большего размера и попытайтесь записать ее в локальное хранилище. Убедитесь в том, что в консоли будет ошибка. 
*/

// let str = '';
// for(let i = 1; i <= 8 * 11 ** 9; i++) {
//    str += '+';
// }
// localStorage.setItem('key', str);//ошибка

//Попытайтесь распарсить некорректный JSON. Убедитесь в том, что в консоли будет ошибка. 


// let res = JSON.parse("{1, 2, 3, 4, 5}");
// console.log(res);




////////////Перехват исключений в JavaScript///////////////////////

/*
 Для перехвата исключений используется конструкция try-catch. Она имеет следующий синтаксис:
try {
	// код
} catch (error) {
	// обработка ошибки
}

В блоке try следует размещать код, который может содержать исключение. Если вдруг при выполнении этого кода возникнет исключительная ситуация, то наш скрипт не рухнет с ошибкой в консоли, а начнет выполнятся код блока catch.

В этом блоке мы должны каким-то адекватным образом отреагировать на ошибку. Например, если мы хотели отправить какие-то данные через интернет и интернет не работает, в блоке catch мы можем как-то справится с ситуацией: можно, например, выдать сообщение пользователю, а можно через некоторое время попытаться отправить данные опять - вдруг интернет уже заработал.

Если же при выполнении блока try никаких исключительных ситуаций не случилось, то полезный код просто выполнится, а код из блока catch - нет.

Для примера давайте попробуем разобрать JSON и в случае его некорректности выведем на экран сообщение об этом:
try {
	let data = JSON.parse('{1,2,3,4,5}');
} catch (error) {
	alert('невозможно выполнить операцию разбора JSON');
}
*/

/*
 Дан код, который записывает некоторую строку в локальное хранилище:
let str = 'некая строка';
localStorage.setItem('key', str);

Оберните этот код в конструкцию try-catch. В блоке catch выведите сообщение о переполнении хранилища. Проверьте работу вашего кода для строки размером менее 5 мб и для строки большего размера. 
*/


// let str = 'a'.repeat(6*1024*1024);
// let str = 'aijhoidfj';
// try {
//    localStorage.setItem('key', str);
//    console.log('Данные сохранены');
// } catch(error) {
//    console.log('Ошибка: Хранилище переполнено!');
//     console.log('Текст ошибки:', error.message);
// }


/*
Дан JSON, внутри которого хранится массив. Если этот JSON корректный, то выведите элементы массива в виде списка ul. Если же JSON не корректный, выведите на экран сообщение о том, что на странице случилась ошибка.
*/

// let json = '[1, 2, 3]';
// let ul = document.querySelector('ul');
// localStorage.setItem('array', json);
// try {
//    let arr = JSON.parse(localStorage.getItem('array'));
//    for(let elem of arr) {
//     let li = document.createElement('li');
//     li.textContent = elem;
//     ul.appendChild(li);
//    }
// } catch(error) {
//    console.log('Ошибка');
// }
   

//////////////////Разработка с try-catch в JavaScript/////////////////////

/*
После изучения конструкции try-catch стиль написания вами кода должен изменится. Теперь вы должны все места, в которых может возникнуть исключительная ситуация, оборачивать в try, а в блоке catch прописывать реакцию на это исключение. 
*/

/*
 Дан следующий код:
let str = 'некая строка';
localStorage.setItem('key', str);
alert('успешно сохранено!');

Что не так с этим кодом? Исправьте недостатки этого кода. 
*/
// let str = 'некая строка';
// try {
//    localStorage.setItem('key1', str);
//    alert('успешно сохранено!');
// } catch(error) {
//    console.log('Ошибка');
// }


/*
 Дан следующий код:
let json = '[1,2,3,4,5]';

let arr = JSON.parse(json);

let sum = 0;
for (let elem of arr) {
	sum += +elem;
}

alert(sum);

Что не так с этим кодом? Исправьте недостатки этого кода. 
*/
// let json = '[1,2,3,4,5]';
// let res = localStorage.setItem('hard', json);
// let sum = 0;
// try {
//    let arr = JSON.parse(localStorage.getItem('hard'));
//    for (let elem of arr) {
//       sum += +elem;
//    }
//    alert(sum);
// } catch(error) {
//    console.log('Ошибка');
// }




///////////////Перехват исключений во вложенном коде в JavaScript///////

/*
 Особенность и удобство исключений заключаются в том, что их можно ловить при любом уровне вложенности кода. Посмотрим на примере. Пусть у нас есть функция, сохраняющая данные в локальное хранилище:
function save(str) {
	localStorage.setItem('key', str);
}

Как вы уже знаете, при переполнении хранилища, метод setItem выбросит исключение. Не обязательно, однако, это исключение ловить внутри функции save. Можно обернуть в try каждый вызов самой функции:
try {
	save('некая строка');
} catch (error) {
	alert('закончилось место в локальном хранилище!');
}

*/

/*
 Дана функция, преобразующая JSON в массив:
function getArr(json) {
	return JSON.parse(json);
}

В следующем коде из JSON получают массив:
let arr = getArr('[1,2,3,4,5]');
console.log(arr);

Оберните вызов функции в конструкцию try-catch. 
*/

// function getArr(json) {
//    return JSON.parse(json);
// }

// try {
//    let arr = getArr('[1,2,3,4,5]');
//    console.log(arr);
// } catch(error) {
//    alert('Ошибка');
// }





//////////////Объект с ошибкой в JavaScript/////////////////////

/*
 Если возникает исключительная ситуация, то в первый параметр блока catch попадет объект со случившейся ошибкой. Свойство name этого объекта содержит имя ошибки (фактически ее тип), а свойство message - текст этой ошибки:
try {
	
} catch (error) {
	console.log(error.name); // имя ошибки
	console.log(error.message); // текст ошибки
}
 Если возникает исключительная ситуация, то в первый параметр блока catch попадет объект со случившейся ошибкой. Свойство name этого объекта содержит имя ошибки (фактически ее тип), а свойство message - текст этой ошибки:
try {
	
} catch (error) {
	console.log(error.name); // имя ошибки
	console.log(error.message); // текст ошибки
}

*/

/*
Специально создайте исключительную ситуацию, связанную с попыткой разбора некорректного JSON. Выведите в консоль имя и текст этой ошибки. 
*/

// function getArr(json) {
//    return JSON.parse(json);
// }

// try {
//   let arr = getArr('[1, 2, 3]');
//   console.log(arr);
// } catch(error) {
//   console.log(error.name);
//   console.log(error.message);
// }


/*
Специально создайте исключительную ситуацию, связанную с переполнением локального хранилища. Выведите в консоль имя и текст этой ошибки. 
*/

// function getArr(json) {
//    return JSON.parse(json);
// }

// try {
//   localStorage.setItem('test', 'a'.repeat(6*1024*1024));
// } catch(error) {
//    console.log(error.name);
//    console.log(error.message);
// }
// localStorage.clear();


/////////////Основные типы исключений в JavaScript/////////////////

/*
 Давайте разберем три основных типа исключения, возникающих в JavaScript.

Исключение типа TypeError представляет собой ошибку, возникающую при недопустимом типе для переменной или параметра. 
Исключение типа SyntaxError представляет собой ошибку, возникающую при разборе исходного кода или JSON.

Исключение типа RangeError представляет собой ошибку, возникающую при попытке передать параметром функции число, которое не входит в допустимый диапазон значений этого параметра функции. Оно может возникать при создании массива с неправильной длиной через конструктор Array, или при передаче плохих значений в методы числа Number.toExponential(), Number.toFixed() или Number.toPrecision().

Существуют и другие типы исключений. 
*/


////////////////Применение типов исключений в JavaScript//////////////////

/*
 Пусть у нас есть функция, параметром принимающая JSON с массивом данных и записывающая каждый элемент этого массива в локальное хранилище:
function saveData(json) {
	let arr = JSON.parse(json);
	
	for (let i = 0; i < arr.length; i++) {
		localStorage.setItem(i, arr[i]);
	}
}

В данной функции исключительная ситуация может возникнуть в двух местах: при разборе JSON и при попытке сохранения данных в локальное хранилище.

Пусть, к примеру, в качестве обработки ошибок мы решили выводить некоторое сообщение о проблемах:
try {
	saveData('{1,2,3,4,5}');
} catch (error) {
	alert('какие-то проблемы');
}

Наше сообщение, это, конечно, хорошо, но оно никак не дифференцирует возникшие проблемы. Более логично было бы вывести сообщение о том, какая именно проблема возникла.

Для этого отличим возникшие ошибки по имени:
try {
	saveData('{1,2,3,4,5}');
} catch (error) {
	if (error.name == 'QuotaExceededError') {
		alert('закончилось место в хранилище');
	}
	
	if (error.name == 'SyntaxError') {
		alert('некорректный JSON');
	}
}

*/

/*
Скопируйте код моей функции saveData, а затем не подсматривая в мой код реализуйте описанную мною обработку ошибок. 
*/
// function saveData(json) {
// 	let arr = JSON.parse(json);
	
// 	for (let i = 0; i < arr.length; i++) {
// 		localStorage.setItem(i, arr[i]);
// 	}
// }

// try {
// 	saveData('{1,2,3,4,5}');
// } catch (error) {
// 	if (error.name == 'QuotaExceededError') {
// 		alert('закончилось место в хранилище');
// 	}
	
// 	if (error.name == 'SyntaxError') {
// 		alert('некорректный JSON');
// 	}
// }


/*
Специально по очереди создайте исключительные ситуации, которые могут возникнуть в функции saveData. 
*/

// function saveData(json) {
//    // let a = [];
//    // a.length = -1; // Это гарантированно вызовет RangeError
// 	let arr = JSON.parse(json);
	
// 	for (let i = 0; i < arr.length; i++) {
// 		localStorage.setItem(i, arr[i]);
// 	}
// }

// try {
//    // let bigData = JSON.stringify(['a'.repeat(10 * 1024 * 1024)]);
//    // saveData(bigData);
// 	// saveData(null);
// 	// saveData('[1,2,3,4,5');
// 	saveData('[1,2,3,4,5]');
// } catch (error) {
   
// 	if (error.name == 'QuotaExceededError') {
// 		alert('закончилось место в хранилище');
// 	}
	
// 	if (error.name == 'TypeError') {
// 		alert('некорректный параметр');
// 	}
// 	if (error.name == 'SyntaxError') {
// 		alert('некорректный JSON');
// 	}
// 	if (error.name == 'RangeError') {
// 		alert('Число не входит вдопустимый диапозон');
// 	}
// }


///////////////Выбрасывание исключений в JavaScript///////////////////

/*
 В JavaScript попытка извлечь корень из отрицательного числа не приводит к выбрасыванию исключения:
let result = Math.sqrt(-1);
console.log(result); // выведет NaN

Напишите свою функцию, которая будет извлекать корень из числа и при этом выбрасывать исключение, если корень извлекается из отрицательного числа. 
*/

// function square(num) {
//    if(num < 0) {
//       throw new Error('корень из отрицательного числа');
//    } else {
//       return Math.sqrt(num);
//    }
// }

// try {
//    let res = square(-2);
//    console.log(res);
// } catch(error) {
//    alert('Ошибка: ' + error.message); 
// }



///////////Выбрасывание различных типов исключений в JavaScript//////////

/*
Давайте выбросим свое исключение и посмотрим, как будет вести себя объект с ошибкой в этом случае:
try {
	throw new Error('текст исключения');
   } catch (error) {
      console.log(error.name); // 'Error'
      console.log(error.message); // 'текст исключения'
   }
      
      Как вы видите, тип нашего исключения - 'Error'. Такой же тип будет у любых исключений, выброшенных подобным образом. Это, однако, не всегда будет удобно, так как, если у нас могут быть несколько исключений, мы не сможем их отличить друг от друга.
      
      В JavaScript предусмотрено решение этой проблемы: можно выбрасывать исключения не только типа Error, но и любого встроенного в JavaScript типа ошибки, например, TypeError, SyntaxError, RangeError.
      
      Давайте для примера выбросим исключение типа SyntaxError:
      try {
         throw new SyntaxError('текст исключения');
         } catch (error) {
            console.log(error.name); // 'SyntaxError'
            console.log(error.message); // 'текст исключения'
      }
            
   */
           
  // Выбросите исключение с типом TypeError. 

   // function square(num) {
   //    if(typeof num !== 'number') {
   //       throw new TypeError('не число');
   //    }
   //    if(num > 0) {
   //       return Math.sqrt(num);
   //    }
   // }
   // try {
   //    let res = square('Hello');
   //    console.log(res);
   // } catch(error) {
   //    console.log(error.name);
   //    console.log(error.message); 
   // }
            
            
            
/*
Выбросите исключение с типом SyntaxError и RangeError. Поймайте эти исключения с помощью одного блока try. В блоке catch выведите разные сообщения об ошибке для исключений разных типов. 
*/
// function square(str) {
//   return str + str;
// }
// try {
//    //throw new RangeError('Ошибка диапазона');
//    throw new SyntaxError('Ошибка синтаксиса');
// } catch(error) {
//    if(error.name == 'SyntaxError') {
//       console.log('Поймали ошибку синтаксиса');
//    }
//    if(error.name == 'RangeError') {
//       console.log('Поймали ошибку диапазона');
//    }
// }


/////////////Выбрасывание своих типов исключений в JavaScript///////////

/*
 Количество встроенных в JavaScript исключений не такое большое и они чаще всего не могут удовлетворить все наши потребности в разных типах исключений. Поэтому в JavaScript встроена возможность создавать исключения со своим типом.

Существуют разные способы сделать это. Самый простой - в throw передать объект с ключами name и message:
try {
	throw {name: 'MyError', message: 'текст исключения'};
} catch (error) {
	console.log(error.name); // 'MyError'
	console.log(error.message); // 'текст исключения'
}

*/

/*
 Ранее мы сделали функцию, выбрасывающую исключение при делении на ноль:
function div(a, b) {
	if (b !== 0) {
		return a / b;
	} else {
		throw new Error('ошибка деления на ноль');
	}
}

Переделайте эту функцию так, чтобы она выбрасывала исключение с каким-нибудь придуманными нами типом, например, DivisionByZeroError. 
*/
// function div(a, b) {
// 	if (b === 0) {
//       throw {name: 'myErrorDeevision', message: 'ошибка деления на ноль'};
// 	} else {
// 		return a / b;
// 	}
// }
// try {
//    let res = div(10, 0);
//    console.log(res);
// } catch(error) {
//    console.log(error.name);
//    console.log(error.message);
// }

/*
Ранее вы делали функцию, выбрасывающую исключение при попытке извлечь корень из отрицательного числа. Переделайте вашу функцию так, чтобы она выбрасывала исключение с придуманным вами типом. Хорошо подумайте над названием исключения, чтобы это название было удачным. 
*/

// function square(num) {
//    if(num < 0) {
//       throw {name: "negativNumError", message: 'Отрицательное число'};
//    }
//    return Math.sqrt(num);
// }
// try {
//    let res = square(-5);
//    console.log(res);
// } catch(error) {
//    console.log(error.name);
//    console.log(error.message); 
// }



///////////////Пример исключения с data- атрибутами в JavaScript///////

/*
 Пусть при загрузке страницы сервер создает HTML код, в котором хранится название, цена и количество купленного продукта:
<div id="product" data-product="яблоко" data-price="1000" data-amount="5"></div>

Давайте сделаем функцию, которая будет принимать ссылку на элемент с продуктом и находить полную стоимость товара (цену умножать на количество):
function getCost(elem) {
	return elem.dataset.price * elem.dataset.amount;
}

Найдем стоимость нашего продукта:
let product = document.querySelector('#product');
let cost = getCost(product);

alert(cost);

Предположим теперь следующую ситуацию: из-за какого-то сбоя на сервере он прислал нам товар, в котором отсутствует цена или количество (или оба сразу), например, вот так:
<div id="product" data-product="яблоко" data-price="1000"></div>

Если теперь попробовать посчитать стоимость товара, то результате на экран выведется NaN. Согласитесь, не очень информативно.

Получается, нам нужно как-то обезопасится от того, что будут отсутствовать нужные нам атрибуты. Это можно сделать двумя путями. Первый путь - это сказать, что это нормальное поведение и просто проверять ифами наличие нужных нам атрибутов:
function getCost(elem) {
	if (elem.dataset.price !== undefined && elem.dataset.amount !== undefined) {
		return elem.dataset.price * elem.dataset.amount;
	} else {
		return 0; // вернем что-нибудь, например, 0 или null или false
	}
}

Второй вариант - это сказать, что отсутствие атрибута data-price или data-amount - исключительная ситуация. В этом случае мы будем выбрасывать исключение:
function getCost(elem) {
	if (elem.dataset.price !== undefined && elem.dataset.amount !== undefined) {
		return elem.dataset.price * elem.dataset.amount;
	} else {
		throw {
			name: 'ProductCostError',
			message: 'отсутствует цена или количество у продукта'
		};
	}
}

Какой из двух вариантов здесь уместнее применить - это выбор программиста. Он может считать проблему нормальной работой скрипта или исключительной ситуацией.

Пусть мы решили, что ситуация исключительная. Toгда код получения стоимости товара будет выглядеть вот так:
let product = document.querySelector('#product');

try {
	let cost = getCost(product);
	alert(cost);
} catch (error) {
	// как-то реагируем на исключение
}


*/

/*
Переделайте мой код так, чтобы функция getCost выбрасывала два типа исключений: если отсутствует цена и если отсутствует количество. Хорошо подумайте над названиями этих исключений. В блоке catch выведите разные сообщения об ошибке для исключений разных типов. 
*/

let product = document.querySelector('#product');
function getCost(elem) {
	if (elem.dataset.price !== undefined && elem.dataset.amount !== undefined) {
		return elem.dataset.price * elem.dataset.amount;
	} else {
      if(elem.dataset.price == undefined) {
         throw {
            name: 'ProductCostPriceError',
            message: 'отсутствует цена продукта'
         };
      }
      if(elem.dataset.amount == undefined) {
         throw {
            name: 'ProductCostCountError',
            message: 'отсутствует количество продукта'
         };
      }
	}
}


try {
	let cost = getCost(product);
	alert(cost);
} catch (error) {
      console.log(error.name);
      console.log(error.message);
}

/*
ИИ
function getCost(elem) {
   // Проверяем цену
   if (elem.dataset.price === undefined) {
      throw {
         name: 'ProductCostPriceError',
         message: 'В атрибутах отсутствует цена (data-price)'
      };
   }
   
   // Проверяем количество
   if (elem.dataset.amount === undefined) {
      throw {
         name: 'ProductAmountError', // Немного сократил для красоты
         message: 'В атрибутах отсутствует количество (data-amount)'
      };
   }

   // Если мы дошли досюда, значит всё на месте
   return elem.dataset.price * elem.dataset.amount;
}

try {
   let cost = getCost(product);
   console.log('Общая стоимость:', cost);
} catch (error) {
   // В блоке catch выводим разные сообщения
   if (error.name === 'ProductCostPriceError') {
      alert('Ошибка: Укажите цену товара!');
   } else if (error.name === 'ProductAmountError') {
      alert('Ошибка: Укажите количество товара!');
   } else {
      alert('Произошла неизвестная ошибка: ' + error.message);
   }
}
*/


/////////////////Пример исключения с JSON в JavaScript///////////////

/*
 Пусть к вам приходит JSON вот такого вида:
let json = `[
	{
		"name": "user1",
		"age": 25,
		"salary": 1000
	},
	{
		"name": "user2",
		"age": 26,
		"salary": 2000
	},
	{
		"name": "user3",
		"age": 27,
		"salary": 3000
	}
]`;

Проверьте этот JSON на общую корректность при разборе, а после разбора проверьте, что в результате получается массив, а не что-то другое. Если в результате получается не массив - выбросите исключение. 
*/



/////////////////Проброс исключений в JavaScript//////////////////////
/*
 Дан следующий код:
try {
	let arr = JSON.parse(json);

	for (let i = 0; i < arr.length; i++) {
		localStorage.setItem(i, arr[i]);
	}
} catch (error) {
	if (error.name == 'QuotaExceededError') {
		alert('закончилось место в хранилище');
	}
	
	if (error.name == 'SyntaxError') {
		alert('некорректный JSON');
	}
}

Что не так с этим кодом? Исправьте его на более удачный. 
*/